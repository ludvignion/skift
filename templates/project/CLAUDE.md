## Stack
<!-- Filled by the skift initializer: language, framework, package manager, versions; from the spec's Constraints, or its own choice if the spec is silent. -->

## Run commands
<!-- Filled by the skift initializer: start the app (./init.sh), run the tests, where the log is. -->

## Verification
<!-- The only section you fill, at init: how a session exercises the running app as a user would, e.g. a browser tool, HTTP requests or the CLI. Name the tool here and nowhere else. If it is an MCP server, declare it in .mcp.json and approve it once in an interactive session before running the loop. -->

## Rule
One feature per session. A feature passes only after its steps were observed in the running app.
